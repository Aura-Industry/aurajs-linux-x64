# @aurajs/linux-x64

Platform-specific native host package for AuraJS.

- Target: linux-x64
- Binary path: bin/aurajs-host
- Versioning: must match root aurajs version.

This package is consumed via aurajs optionalDependencies and is not installed directly in normal workflows.
